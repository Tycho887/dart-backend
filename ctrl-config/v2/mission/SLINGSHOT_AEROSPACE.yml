config: SLINGSHOT_AEROSPACE
acu_config: SLINGSHOT_AEROSPACE

services:
  proxy:
    tags:
      - SU
      - SD

    whitelist:
      - 192.146.238.0/24
      - 10.1.15.0/24

    services:
      - qradio_tcp
      - spectrum_http
      - mc_http

  specnet:
    tags:
      - SD
      - SU

    autoconf:
      reference_attenuation_adjustment: 0 
      spectral_class_dl: s_rhcp  

  qradio:
    tags:
      - SU
      - SD
