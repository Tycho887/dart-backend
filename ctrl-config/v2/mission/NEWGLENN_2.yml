---
config: NEWGLENN_2
acu_config: NEWGLENN_2

services:
  proxy:
    tags:
      - SD

    whitelist:
      - 192.146.238.242
      - 100.64.77.0/24 

    services:
      - ctx_tcp # Customer will use ctx
      - qradio_tcp # adding qradio just in case
      - spectrum_http
      - mc_http
      - monitor_tcp

  specnet:
    tags:
      - SD

    autoconf:
      txEnable: False
      reference_attenuation_adjustment: 0
      system_bandwidth: "22.0"
      inputFrequency: 70
      streamOffset: 0
      streamBandwidth: 20
  
  qradio:
    tags:
      - SD
