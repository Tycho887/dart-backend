---
acu_config: SARSAT_2_RHCP
config: SARSAT_2_HIGH

services:

  custom_backend:
    tags:
      - SD
      - SU

    system_specific:
      MN4:
        mtx_tags_downlink:
          - [DL, CTX1]
          - [DR, CTX2]

        mtx_tags_uplink:
          - [UCTX, U]

  proxy:
    tags:
      - SU
      - SD

    whitelist:
      - 10.141.209.42 # ADD vpn adress
      - 10.141.209.41 # ADD vpn adress
      - 10.141.209.40 # ADD vpn adress
      - 172.31.244.25 # saa-nix
      #need input from IT what IP adress to whitelist from site proxy to access proxy

    services:
      - ctx_tcp
      - monitor_tcp

  specnet:
    tags:
      - SD
      - SU

  #qradio:
  #  tags:
  #    - SD
  #    - SU
#
  #qfep:
  #  config: SARSAT_1_HIGH
  #  tags:
  #    - SD
  #    - SU
