---
config: AWS_LBAND
acu_config: AWS_LBAND

services:

  proxy:
    tags:
      - SU
      - SD

    whitelist:

    services:
      - qradio_tcp

  specnet:
    tags:
      - SD
      - SU

    autoconf:
      reference_attenuation_adjustment: -5

  qradio:
    tags:
      - SD
      - SU

  qfep:
    tags:
      - SD
      - SU

  
