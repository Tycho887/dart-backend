---
config: GPDM_50_100
acu_config: GPDM

services:

  proxy:
    tags:
      - SU
      - SD

    whitelist:
      - 100.64.140.0/24 # SD-WAN

    services:
      - qradio_tcp
      - mc_http
      - spectrum_http

  specnet:
    tags:
      - SD
      - SU

    autoconf:
      enabled: True
      reference_attenuation_adjustment: 1

  qradio:
    tags:
      - SD
      - SU

  
