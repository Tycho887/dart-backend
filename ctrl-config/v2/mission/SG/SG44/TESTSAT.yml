---
acu_config: JPSS_1
config: JPSS_1_mod1

services:
  qmr:
    tags:
      - LD
      - XD

  qdra-inst1:
    tags:
      - LD

    record: True

    transfer_staging:
      stream_raw: False
      backend: aws_s3
      home:
        # -------------------------------------------------------------------------
        # Putting s3 parameters in home-dict for now
        # In qfte, the ftp_home variable will be a dict containing the values below
        # -  Birk
        # -------------------------------------------------------------------------

        bucket: arn:aws:s3:us-east-1:412574793948:accesspoint/birksat-access-point
        accelerate: False
        prefix: dev/sg44-birk
        profile: birk
        extension: .cadu
