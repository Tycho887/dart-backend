---
config: FALCON9
acu_config: FALCON9
customer: SPACEX

check_tle: False # While investigating false alarm

services:

  proxy:
    tags:
      - SD

    whitelist:

    services:
      - qradio_tcp
      - spectrum_http
      - mc_http

  specnet:
    tags:
      - SD

    autoconf:
      reference_attenuation_adjustment: 0

  qradio:
    tags:
      - SD
