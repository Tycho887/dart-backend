---
config: default

services:
  specnet:
    tags:
      - SD

    autoconf:
      reference_attenuation_adjustment: -5
      spectral_class_dl: s_lhcp
      txEnable: False

  qradio:
    tags:
      - SD
