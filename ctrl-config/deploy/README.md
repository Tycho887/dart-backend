# Ansible push to mission configs to schedulers

Run ./deploy from bash. You will need to run ssh-copy-id lite@scheduler-ip before you can do this.

Specify scheduler from hosts.yml file or write schedulers to push to all

This will pull master branch of ctrl-config to schedulers

