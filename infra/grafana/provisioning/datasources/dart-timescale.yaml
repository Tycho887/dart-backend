apiVersion: 1

datasources:
  - name: DART TimescaleDB
    uid: ffo3xwq8aqt4we
    type: postgres
    access: proxy
    url: timescaledb:5432
    user: ${POSTGRES_USER}
    secureJsonData:
      password: ${POSTGRES_PASSWORD}
    jsonData:
      database: ${POSTGRES_DB}
      sslmode: disable
      postgresVersion: 1500
      timescaledb: true
    editable: false
